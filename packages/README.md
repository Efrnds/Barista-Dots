# Pacotes Barista Dots

| Arquivo | Uso |
|---------|-----|
| `core.txt` | Obrigatórios (~35) — `./install.sh` |
| `extras.txt` | Dump pessoal opcional — `./install.sh --extras` |

Atualize `extras.txt` com `~/dotfiles/import.sh` antes de commitar.
